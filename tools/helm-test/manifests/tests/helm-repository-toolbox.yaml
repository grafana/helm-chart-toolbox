---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: helm-chart-toolbox
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/grafana/helm-chart-toolbox
  ref:
    # TEMPORARY: pointed at the branch that adds remotecfg-components-test so it can
    # be tested locally before merge. REVERT to `main` before opening/merging the PR.
    branch: add-remotecfg-components-test
