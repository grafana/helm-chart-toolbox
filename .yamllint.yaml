---
yaml-files:
  - "*.yaml"
  - "*.yml"
  - ".yamllint"

ignore:
  - .git
  - charts/**/templates
  - kubeconfig.yaml
  - node_modules

extends: default

rules:
  line-length:
    max: 200
    level: warning
